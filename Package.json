{
  "name": "emden-rp-admin-panel",
  "version": "1.0.0",
  "description": "Admin panel for Emden RP server.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "uuid": "^9.0.1"
    // 'node-fetch' ist in Vercel/Node 18+ nicht explizit nötig, 
    // da 'fetch' global verfügbar ist.
  }
}